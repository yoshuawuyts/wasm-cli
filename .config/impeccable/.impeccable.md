# Design Guidelines

## Design Context

### Users

WebAssembly developers building with Components and WIT interfaces. They use
`wasm-cli` from the terminal to install, publish, and run Wasm packages — and
browse the web registry to discover what's available. Their context is
development workflow: fast iteration, dependency management, and composing
components. The job to be done is **finding, installing, and shipping Wasm
packages with zero friction**.

### Brand Personality

**Minimal, Clean, Professional.**

The tone is direct and technical — like well-written documentation. No marketing
speak, no unnecessary flair. Information density is preferred over decoration.
The tool should feel like a sharp, reliable instrument that stays out of the way.

### Aesthetic Direction

- **Visual tone**: Technical minimalism. Generous whitespace, clear typography
  hierarchy, restrained color use.
- **Reference**: [zed.dev/extensions](https://zed.dev/extensions) — clean grid
  of packages, search-first, fast-loading, no noise.
- **Anti-reference**: Flashy startup landing pages — style over substance,
  gratuitous animation, marketing-heavy layouts.
- **Emotional goal**: Speed — "this feels fast and efficient."
- **Theme**: Light only. No dark mode.
  Standard terminal palette for CLI/TUI.

### Color System

Two-tone palette inspired by [charcuterie.elastiq.ch](https://charcuterie.elastiq.ch).
Neutral gray background (`#d9d9d9`), vivid blue foreground (`#232cf4`).
Hierarchy comes from opacity, not different hues. No dark mode.

| Token          | Value                          | Usage                                        |
|----------------|--------------------------------|----------------------------------------------|
| Background     | `#d9d9d9`                      | Page background                              |
| Foreground     | `#232cf4`                      | Body text, headings, links, accents          |
| Secondary      | `rgba(35, 44, 244, 0.85)`      | Descriptions, metadata                       |
| Muted          | `rgba(35, 44, 244, 0.70)`      | Timestamps, help text                        |
| Faint          | `rgba(35, 44, 244, 0.4)`       | Version numbers, counts, minor labels        |
| Border         | `rgba(35, 44, 244, 0.25)`      | Card borders, dividers                       |
| Border light   | `rgba(35, 44, 244, 0.12)`      | Subtle dividers                              |
| Surface        | `#cfcfcf`                      | Inline code backgrounds, hover surfaces      |
| Surface muted  | `#c8c8c8`                      | Subdued surface variant                      |

**CLI colors**: Green (success/checkmarks), Yellow (active/progress bars),
Dim (secondary info like versions and sizes).

**TUI colors**: Yellow (active state, headers), Cyan (titles, counts),
Dark Gray (help text, disabled), Magenta/Blue/Green (WIT syntax highlighting).

### Typography

- **Font family**: Iosevka Web (self-hosted, woff2) for both sans and mono.
  Four weights loaded: Regular (400), Medium (500), SemiBold (600), Bold (700).
- **Weight strategy**: Headings use `font-normal` (400) or `font-light` (300)
  — never bold. Labels and emphasis use `font-medium` (500). Reserve heavier
  weights for rare, intentional emphasis.
- **Tracking**: Headings use `tracking-display` (-0.06em) for tighter, more
  refined display type. Body text uses default tracking.
- **Rendering**: `antialiased` on body for crisp glyph rendering.
- Monospace for package names, code references, versions, and WIT identifiers.
- Size scale: `text-sm` (14px) is the floor — all body text, metadata, labels,
  and detail use it. Larger sizes (`text-lg`, `text-xl`, `text-2xl`, `text-3xl`,
  `text-5xl`) provide hierarchy for headings and hero. Never use `text-xs`.

### Motion & Effects

- **No glow, no fade, no entrance animations.** Prefer clear lines, crisp
  borders, and legible text over decorative effects.
- Subtle hover effects are acceptable: border color shifts, background tints,
  underlines. Keep them instantaneous or ≤150ms `transition-colors`.
- No box-shadows for interactive feedback on detail pages. Use border-color
  changes instead. Box-shadows with hover scaling (`card-lift`) are acceptable
  on landing and listing pages for card hover effects.
- No staggered card entrance animations. Content appears immediately.
- Typography is the primary design tool — weight, size, tracking, and color
  do all the heavy lifting.

### Design Principles

1. **Content over chrome** — Every pixel should serve a purpose. No decorative 
   elements that don't aid comprehension. Package data is the interface.

2. **Speed is a feature** — The UI should feel instant. Server-side rendering,
   minimal JavaScript, aggressive caching, progress indicators that show real
   work happening. No loading spinners hiding slow backends.

3. **Terminal-native** — The CLI and TUI are first-class interfaces, not
   afterthoughts. Respect `NO_COLOR`, use standard terminal conventions, and
   prefer information density over visual embellishment.

4. **Consistency across surfaces** — The same package should feel recognizable
   whether viewed in the CLI, TUI, or web frontend. Use the same naming
   conventions (`namespace:name`), the same data hierarchy, and the same
   accent color where applicable.

5. **Accessible by default** — Standard accessibility best practices: sufficient
   color contrast, semantic HTML, keyboard navigation, respect for user
   preferences (color, motion).

### Learned Preferences

- **Strip, don't add.** When something feels wrong, the answer is almost always
  to remove elements rather than add new ones. The header bar was removed
  entirely in favor of a minimal transparent nav. The subtitle was removed. The
  CTA was demoted from a button to a text link. Less is more — always.
- **No chrome.** Background colors on nav bars, colored headers, decorative
  borders — all rejected. The nav should be invisible: just a wordmark and a
  few text links floating at the top. No background, no border, no visual weight.
- **Hierarchy through spacing, not decoration.** Sections are separated by
  generous whitespace (`mb-10`, `pb-16`), not borders or background bands. Group
  related elements tightly and separate groups with breathing room.
- **Text links over buttons for secondary actions.** "Publish a component →" is a
  text link, not a button. Only the search submit gets button treatment. Keep
  visual weight proportional to importance.
- **Cards over rows.** Package listings use a responsive card grid
  (1 / 2 / 3 columns), not a vertical list of rows. Reference: zed.dev/extensions.
- **Search is the hero.** The search bar is the primary interactive element on
  the landing page — autofocused, prominent, with the package count baked into
  the placeholder text. Everything else supports discovery.
- **Monospace for identity.** Package names (`wasi:http`), commands
  (`wasm install`), and versions use monospace. This isn't decoration — it
  signals "this is something you type."
- **Dark mode is not an afterthought.** Every color token must pass WCAG AA in
  both themes. The accent purple needs to be lighter in dark mode
  (`oklch(0.70 0.16 280)`) to maintain contrast. Test both themes.
